<%= form_for(@worker) do |f| %>
  

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  
  <!-- Text box for deferral reason here -->
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
